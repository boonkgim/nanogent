# nanogent — local secrets + runtime state. Everything else in this
# directory (nanogent.mjs, config.json, prompt.md, tools/, Dockerfile,
# docker-compose.yml) is safe to commit so the team can share it.

.env
state/
