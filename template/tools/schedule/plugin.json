{
  "name": "schedule",
  "type": "tools",
  "description": "Agent-facing tool + self-contained proactive tick loop for time-based schedules (definitions and execution log live under this plugin's own state/ dir)",
  "files": ["index.ts", "README.md", "gitignore"]
}
